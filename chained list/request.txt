（1）以Node为结点，并提供不少于5条记录，分别用头插法、尾插法实现链表存储。
struct Node {
int dat;
Node *next;
};
（2）在上题（1）中，设计算法，通过一趟遍历在单链表中确定值最大的结点。
（3）在上题（1）中，设计算法，删除递增有序链表中值大于mink且小于maxk的所有元素（mink和maxk是给定的两个参数，其值可以和表中的元素相同，也可以不同 ）。
